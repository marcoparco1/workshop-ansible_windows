---
- hosts: windows
  name: Prueba
  become: true
  ansible_become: false
  
  tasks:
    - name: Show hostname
      debug:
        msg: "Hostname: {{ ansible_hostname }} "
        
    - name: run the setup facts
      ansible.builtin.setup:
        
